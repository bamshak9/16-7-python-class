This README is for the file logical.py
The file is a very simple calculator program which can handle the operations: addition, subtraction, multiplication, division and can carry out exponential operations.
The program executes in step, with addition first to subtraction, then multiplication, division before exponential. 
At the moment, it can only receive input for two values at a time.
The answers are programmed to be displayed to 2 dp only.
